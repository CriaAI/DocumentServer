# ═══════════════════════════════════════════════════════════════
# CriaAI DocumentServer - Environment Variables
# For local/docker-compose: copy to .env and fill in values
# For ECS: these are set via task definition + Secrets Manager
# ═══════════════════════════════════════════════════════════════

# ── JWT Authentication (REQUIRED - change these!) ──
JWT_ENABLED=true
JWT_SECRET=your-very-long-random-secret-here
JWT_HEADER=Authorization
JWT_IN_BODY=true

# ── Domain & SSL (docker-compose only, not needed for ECS) ──
# ECS uses ALB + ACM for HTTPS instead
DOMAIN=docs.yourdomain.com
CERT_EMAIL=admin@yourdomain.com
