# ═══════════════════════════════════════════════════════════════
# CriaAI DocumentServer - Production Environment Variables
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════

# ── JWT Authentication (REQUIRED - change these!) ──
JWT_ENABLED=true
JWT_SECRET=your-very-long-random-secret-here
JWT_HEADER=Authorization
JWT_IN_BODY=true

# ── Domain & SSL (for Let's Encrypt) ──
# Leave DOMAIN empty to use HTTP only (or self-signed cert)
DOMAIN=docs.yourdomain.com
CERT_EMAIL=admin@yourdomain.com
